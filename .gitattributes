# Normalize all text files to LF in the repo, regardless of OS.
# Git will check out with native line endings on Windows; this just keeps
# the stored version consistent across contributors.
* text=auto eol=lf

# Treat binary files as binary (no line-ending munging)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.webp binary
*.zip binary
